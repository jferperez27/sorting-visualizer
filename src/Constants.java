public class Constants {

    // Predetermined Window Length
    public static final int WINDOW_LENGTH = 1600; // Default: 1600

    // Predetermined Window Width
    public static final int WINDOW_HEIGHT = 1000; // Default: 1000

    // Max amount of array index (length)
    public static final int ARRAY_MAX_LENGTH = 178; // Default: 178

    // MAX value possible per individual array value
    public static final int ARRAY_MAX_VALUE = 750; // Default: 750

    // MIN value possible per individual array value
    public static final int ARRAY_MIN_VALUE = 0; // Default: 0

    // Bar preset length
    public static final int ARRAY_REC_LENGTH = 8; // Default: 8

    // Bar padding
    public static final int ARRAY_PADDING = 1; // Default: 1

    // Sorting speed (PaneOrganizer timeline speed)
    public static double SORT_SPEED = 0.25; // Default: 0.25

    // Sorting speed (InsertionSort timeline speed)
    public static double WHILE_LOOP_SPEED = 1; // Default: 0.25
}
